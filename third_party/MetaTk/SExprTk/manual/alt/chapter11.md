# Chapter 11: Serialization

Serialization paragraph 1. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 2. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 3. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 4. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 5. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 6. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 7. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 8. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 9. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 10. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 11. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 12. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 13. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 14. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 15. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 16. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 17. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 18. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

```text
(alpha beta gamma)
```

Serialization paragraph 19. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 20. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 21. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 22. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 23. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 24. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 25. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 26. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 27. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 28. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 29. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 30. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 31. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 32. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 33. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 34. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 35. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 36. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

```cpp
auto cartable = sexprtk::SExprTk{}.parse(sexprtk::Source::from_string("(k v)"));
```

Serialization paragraph 37. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 38. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 39. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 40. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 41. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 42. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 43. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 44. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 45. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 46. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 47. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 48. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 49. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 50. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 51. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 52. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 53. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 54. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

```text
1|atom|payload
```

Serialization paragraph 55. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 56. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 57. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 58. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 59. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 60. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 61. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 62. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 63. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 64. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 65. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 66. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 67. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 68. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 69. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 70. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 71. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

Serialization paragraph 72. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the serialization surface.

```cpp
sexprtk_xas::DatagramFrame::from_event({7, sexprtk_xas::EventType::Atom, "x"});
```
