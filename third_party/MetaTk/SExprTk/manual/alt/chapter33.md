# Chapter 33: Testing

Testing paragraph 1. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 2. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 3. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 4. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 5. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 6. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 7. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 8. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 9. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 10. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 11. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 12. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 13. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 14. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 15. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 16. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 17. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 18. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

```text
(alpha beta gamma)
```

Testing paragraph 19. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 20. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 21. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 22. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 23. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 24. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 25. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 26. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 27. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 28. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 29. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 30. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 31. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 32. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 33. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 34. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 35. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 36. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

```cpp
auto cartable = sexprtk::SExprTk{}.parse(sexprtk::Source::from_string("(k v)"));
```

Testing paragraph 37. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 38. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 39. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 40. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 41. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 42. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 43. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 44. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 45. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 46. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 47. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 48. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 49. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 50. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 51. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 52. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 53. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 54. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

```text
1|atom|payload
```

Testing paragraph 55. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 56. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 57. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 58. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 59. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 60. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 61. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 62. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 63. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 64. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 65. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 66. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 67. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 68. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 69. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 70. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 71. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

Testing paragraph 72. SExprTk keeps semantic structure explicit, preserves list topology, and treats surface syntax as transport rather than authority. The implementation favors deterministic parsing, reproducible serialization, shallow extension points, and a compact event protocol suitable for streaming, testing, and cross-process exchange without hidden runtime state. This chapter instance records invariants, layer boundaries, canonical forms, and failure handling rules for the testing surface.

```cpp
sexprtk_xas::DatagramFrame::from_event({7, sexprtk_xas::EventType::Atom, "x"});
```
